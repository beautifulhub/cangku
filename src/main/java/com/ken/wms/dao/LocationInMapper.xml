<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ken.wms.dao.LocationInMapper">
	
	<resultMap type="com.ken.wms.domain.LocationInDO" id="locationInDO">
		<id property="id" column="STOCKIN_ID"/>
		<result property="supplierID" column="STOCKIN_SUPPLIER_ID"/>
		<result property="supplierName" column="STOCKIN_SUPPLIER_NAME"/>
		<result property="goodsID" column="STOCKIN_GOODS_ID"/>
		<result property="goodsName" column="STOCKIN_GOODS_NAME"/>
		<result property="goodsNum" column="STOCKIN_NUMBER"/>
		<result property="time" column="STOCKIN_TIME"/>
		<result property="personInCharge" column="STOCKIN_PERSON"/>
		<result property="repositoryID" column="STOCKIN_REPOSITORY_ID"/>
	</resultMap>
	
	<select id="selectAll" resultMap="locationInDO">
		select
			r.RECORD_ID as STOCKIN_ID,
			r.RECORD_SUPPLIERID as STOCKIN_SUPPLIER_ID,
			s.SUPPLIER_NAME as STOCKIN_SUPPLIER_NAME,
			r.RECORD_GOODSID as STOCKIN_GOODS_ID,
			g.GOODS_NAME as STOCKIN_GOODS_NAME,
			r.RECORD_NUMBER as STOCKIN_NUMBER,
			r.RECORD_TIME as STOCKIN_TIME,
			r.RECORD_PERSON as STOCKIN_PERSON,
			r.RECORD_REPOSITORYID as STOCKIN_REPOSITORY_ID
		from
			wms_record_in r,wms_goods g,wms_supplier s
		where
			r.RECORD_SUPPLIERID = s.SUPPLIER_ID and
			r.RECORD_GOODSID = g.GOODS_ID
	</select>
	
	<select id="selectBySupplierId" resultMap="locationInDO">
		select
			r.RECORD_ID as STOCKIN_ID,
			r.RECORD_SUPPLIERID as STOCKIN_SUPPLIER_ID,
			s.SUPPLIER_NAME as STOCKIN_SUPPLIER_NAME,
			r.RECORD_GOODSID as STOCKIN_GOODS_ID,
			g.GOODS_NAME as STOCKIN_GOODS_NAME,
			r.RECORD_NUMBER as STOCKIN_NUMBER,
			r.RECORD_TIME as STOCKIN_TIME,
			r.RECORD_PERSON as STOCKIN_PERSON,
			r.RECORD_REPOSITORYID as STOCKIN_REPOSITORY_ID
		from
			wms_record_in r,wms_goods g,wms_supplier s
		where
			r.RECORD_SUPPLIERID = s.SUPPLIER_ID and
			r.RECORD_GOODSID = g.GOODS_ID and
			r.RECORD_SUPPLIERID = #{supplierID}
	</select>
	
	<select id="selectByGoodsID" resultMap="locationInDO">
		select
			r.RECORD_ID as STOCKIN_ID,
			r.RECORD_SUPPLIERID as STOCKIN_SUPPLIER_ID,
			s.SUPPLIER_NAME as STOCKIN_SUPPLIER_NAME,
			r.RECORD_GOODSID as STOCKIN_GOODS_ID,
			g.GOODS_NAME as STOCKIN_GOODS_NAME,
			r.RECORD_NUMBER as STOCKIN_NUMBER,
			r.RECORD_TIME as STOCKIN_TIME,
			r.RECORD_PERSON as STOCKIN_PERSON,
			r.RECORD_REPOSITORYID as STOCKIN_REPOSITORY_ID
		from
			wms_record_in r,wms_goods g,wms_supplier s
		where
			r.RECORD_SUPPLIERID = s.SUPPLIER_ID and
			r.RECORD_GOODSID = g.GOODS_ID and
			r.RECORD_GOODSID = #{goodsID}
	</select>
	
	<select id="selectByID" resultMap="locationInDO">
		select
			r.RECORD_ID as STOCKIN_ID,
			r.RECORD_SUPPLIERID as STOCKIN_SUPPLIER_ID,
			s.SUPPLIER_NAME as STOCKIN_SUPPLIER_NAME,
			r.RECORD_GOODSID as STOCKIN_GOODS_ID,
			g.GOODS_NAME as STOCKIN_GOODS_NAME,
			r.RECORD_NUMBER as STOCKIN_NUMBER,
			r.RECORD_TIME as STOCKIN_TIME,
			r.RECORD_PERSON as STOCKIN_PERSON,
			r.RECORD_REPOSITORYID as STOCKIN_REPOSITORY_ID
		from
			wms_record_in r,wms_goods g,wms_supplier s
		where
			r.RECORD_SUPPLIERID = s.SUPPLIER_ID and
			r.RECORD_GOODSID = g.GOODS_ID and
			r.RECORD_ID = #{id}
	</select>
	
	<select id="selectByRepositoryID" resultMap="locationInDO">
		select
			r.RECORD_ID as STOCKIN_ID,
			r.RECORD_SUPPLIERID as STOCKIN_SUPPLIER_ID,
			s.SUPPLIER_NAME as STOCKIN_SUPPLIER_NAME,
			r.RECORD_GOODSID as STOCKIN_GOODS_ID,
			g.GOODS_NAME as STOCKIN_GOODS_NAME,
			r.RECORD_NUMBER as STOCKIN_NUMBER,
			r.RECORD_TIME as STOCKIN_TIME,
			r.RECORD_PERSON as STOCKIN_PERSON,
			r.RECORD_REPOSITORYID as STOCKIN_REPOSITORY_ID
		from
			wms_record_in r,wms_goods g,wms_supplier s
		where
			r.RECORD_SUPPLIERID = s.SUPPLIER_ID and
			r.RECORD_GOODSID = g.GOODS_ID and
			r.RECORD_REPOSITORYID = #{repositoryID}
	</select>

	<select id="selectByRepositoryIDAndDate" resultMap="locationInDO">
        select
            r.RECORD_ID as STOCKIN_ID,
            r.RECORD_SUPPLIERID as STOCKIN_SUPPLIER_ID,
            s.SUPPLIER_NAME as STOCKIN_SUPPLIER_NAME,
            r.RECORD_GOODSID as STOCKIN_GOODS_ID,
            g.GOODS_NAME as STOCKIN_GOODS_NAME,
            r.RECORD_NUMBER as STOCKIN_NUMBER,
            r.RECORD_TIME as STOCKIN_TIME,
            r.RECORD_PERSON as STOCKIN_PERSON,
            r.RECORD_REPOSITORYID as STOCKIN_REPOSITORY_ID
        from
            wms_record_in r,wms_goods g,wms_supplier s
        where
            r.RECORD_SUPPLIERID = s.SUPPLIER_ID and
            r.RECORD_GOODSID = g.GOODS_ID
        <if test="repositoryID > 0">
            and r.RECORD_REPOSITORYID = #{repositoryID}
        </if>
        <if test="startDate != null">
            and r.RECORD_TIME >= #{startDate}
        </if>
        <if test="endDate != null">
            <![CDATA[and r.RECORD_TIME <= #{endDate}]]>
        </if>
	</select>
	
	<insert id="insert">
		insert into wms_record_in(RECORD_SUPPLIERID,RECORD_GOODS_ID,RECORD_GOODS_COLOR,RECORD_GOODS_SIZE,RECORD_GOODS_NUM,RECORD_TIME,RECORD_PERSON,RECORD_REPOSITORYID)
		values(#{supplierID},#{goodsID},#{goodsColor},#{goodsSize},#{goodsNum},#{time},#{personInCharge},#{repositoryID})
	</insert>
	
	<update id="update">
		update 
			wms_record_in
		set
			RECORD_SUPPLIERID = #{supplierID},
			RECORD_GOODSID = #{goodsID},
			RECORD_NUMBER = #{number},
			RECORD_TIME = #{time},
			RECORD_PERSON = #{personInCharge}
		where
			RECORD_ID = #{id}
	</update>
	
	<delete id="deleteByID">
		delete from wms_record_in
		where RECORD_ID = #{id}
	</delete>
</mapper>